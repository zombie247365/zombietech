{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && cd packages/shared && npx tsc && cd ../database && npx prisma generate && npx tsc && cd ../api && npx tsc"
  },
  "deploy": {
    "startCommand": "node packages/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
